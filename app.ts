let skill: [number, string] = [3, "TypeScript"];
const [progress, skillName] = skill;
// const progress: number = skill[0];
// const skillName: string = skill[1];
console.log(`${skillName} is aquired about ${progress}%`);